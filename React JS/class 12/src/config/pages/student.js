import { Typography } from '@mui/material';
import React from 'react';

export default function Student(){
    return (
        <div>
            <Typography sx={{textAlign:"center"}} variant='h1'>Student Page</Typography>
        </div>
    );
}

