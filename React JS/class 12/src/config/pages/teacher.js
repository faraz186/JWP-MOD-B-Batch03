import { Typography } from '@mui/material';
import React from 'react';

export default function Teacher()
{
    return(
        <div>
            <Typography sx={{textAlign:"center"}} variant='h1'>Teacher Page</Typography>
        </div>

    )
}